// Scenario — the acceptance harness: a YAML scenario DSL parser + a runner that
// drives the real pipeline and asserts an exact alert sequence (SPEC §6.2/§6.5).
// Phase 0 deliverable; placeholder until the parser + runner land.

/// Module marker; superseded by the scenario parser + runner.
public enum ScenarioModule {
    public static let name = "Scenario"
}
