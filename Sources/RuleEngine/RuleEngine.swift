// RuleEngine — typed rules + config hierarchy + alert state machine (SPEC §2.6).
// Phase 2 work; Phase 0 placeholder.

/// Module marker; superseded by the rule evaluation pipeline.
public enum RuleEngineModule {
    public static let name = "RuleEngine"
}
