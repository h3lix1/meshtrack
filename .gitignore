# SwiftPM / build
.build/
.swiftpm/
*.xcodeproj
DerivedData/

# Built app bundle (assembled by `make app` / scripts/make-app.sh)
*.app/

# Coverage / profiling
*.profraw
*.profdata

# Mutation testing
.muter/
muter-report*

# macOS
.DS_Store

# Local secrets / config (secrets live in Keychain, never the repo)
*.local.env
secrets.json

# Editor
.vscode/
.idea/

# Claude Code local state (settings + transient agent worktrees)
.claude/
